# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요
코딩 연습·학습을 위한 폴더입니다. 작은 예제를 여러 개 만들어 보며 배우는 공간이며, 기술 스택은 아직 정해지지 않았습니다.
- 현재 예제: `hello.html` (브라우저에서 바로 여는 정적 HTML 페이지)

## 작업 규칙
- 모든 답변과 문서는 한글로 작성합니다. 명령어·파일 경로·코드 식별자는 원문 그대로 둡니다.
- 코드 주석도 한글로 작성합니다.
- 사용자는 학습 중이므로, 무엇을 왜 바꿨는지 초보자도 이해할 수 있게 쉬운 말로 설명합니다.
- 파일 생성·수정은 이 폴더(`VibeCoding`) 안에서만 합니다.

## 실행 방법
- 로컬 확인: HTML 파일은 파일 탐색기에서 더블클릭하거나 브라우저로 끌어다 놓으면 열립니다.
- 새 기술(예: npm, Python)을 쓰기 시작하면 설치·실행 명령을 이 섹션에 추가합니다.

## 인터넷 공개 (GitHub Pages)
- 저장소: https://github.com/repute82/repute82.github.io (`main` 브랜치의 루트 폴더를 그대로 배포)
- 공개 주소: https://repute82.github.io/ (`index.html`이 `hello.html`로 자동 이동)
- 저장소 이름이 `계정이름.github.io` 형태라서 "사용자 사이트"로 배포되며, 주소 뒤에 폴더명이 붙지 않습니다.
- 로컬 폴더 이름은 `VibeCoding` 그대로이고, 저장소 이름과 달라도 동작에 문제 없습니다.
- 반영 방법: 파일 수정 → `git add` → `git commit` → `git push` → 1~2분 뒤 사이트에 반영
- 저장소가 공개(Public)이므로 올리는 파일은 누구나 볼 수 있습니다. 비밀번호·개인정보는 올리지 않습니다.
- commit 이메일은 이 폴더에만 `repute82@users.noreply.github.com`으로 설정되어 있습니다.
- push는 외부에 공개되는 작업이므로 실행 전에 사용자에게 확인을 받습니다.
